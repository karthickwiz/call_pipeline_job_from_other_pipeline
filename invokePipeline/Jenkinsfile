pipeline {
    agent any
     stages {
        stage('Initialize') {
            steps {
                echo 'setting...'   
                 bat 'set'
            }
        }
       stage('build') {
          steps {                                
             bat 'echo building....'
             bat 'gradle build'
               bat 'echo builded....'    
            
          }
       }
         stage ("invoking clsd pipeline") {
            steps {
                build job: 'clsd', parameters: [
                string(name: 'appId', value: "12")
                ]
            }
        }
    }
    post { 
        always { 
           
             echo 'Done!'
        }
    }
}
