pipeline {
    agent any
     stages {
        stage('Initialize') {
            steps {
                echo 'setting...'   
                 bat 'set'
            }
        }
       stage('build') {
          steps {                                
             bat 'echo building....'
             bat 'gradle build'
               bat 'echo builded....'    
            
          }
       }
         stage ("invoking clsd pipeline") {
            steps {
                echo "$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$"
                build job: 'clsd'
            }
        }
         
           stage ("invoking cdus pipeline") {
            steps {
                echo "$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$"
                build job: 'cdus'
            }
        }
    }
    post { 
        always { 
           
             echo 'Done!'
        }
    }
}
